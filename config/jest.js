// jest.mock('../src/components/modal', () => '<Modal/>');
// jest.mock('../src/components/text-editor', () => '<TextEditor/>');
// // TODO create mocks files to mock jQuery and react-modal
// jest.mock('history');

global.MOCKED = 'mocked';
// global.localStorage = {
//   getItem: jest.fn(),
//   setItem: jest.fn(),
// };
//
// console.error = jest.fn((error) => {
//   if (
//     error.indexOf &&
//     error.indexOf(
//       'React.createElement: type is invalid -- expected a string (for built-in components) or a class',
//     ) === 0
//   ) {
//     return null;
//   }
//   return error;
// });
//
// global.requestAnimationFrame = (callback) => {
//   setTimeout(callback, 0);
// };
